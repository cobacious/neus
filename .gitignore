# Root .gitignore for Neus monorepo
node_modules/
.env
/generated/prisma
# pnpm
.pnpm-debug.log*
# npm (project uses pnpm)
package-lock.json
# Ignore all .env files in subfolders
**/.env
# Ignore all node_modules in subfolders (redundant, but safe)
**/node_modules/
# Ignore build outputs
**/dist/
# Ignore local SQLite dev database
**/*.db
**/*.db-journal
# IDEs
.vscode/*

# AI tool caches
**/.claude/
**/.openai/
